'use strict';

// const private = 'string'; //reserve keyword
// console.log(private);

// const interface = 'string';  //reserve keyword
// console.log(interface);


// Write a simple function to print any string

function preethi (){
    console.log('WBD employee');
}
preethi();
preethi();

// Write a function to add 2 numbers

function calculation (a,b){
    return a+b;
}

console.log(calculation(10,20));

// Write a function using if/else statement

function calc (num){
    if (num > 20){
    return `${num} is greater than 20 `;
    }
    else{
        return `${num} is less than 20`;
    }
}

const value = (calc(50));
console.log(value);

