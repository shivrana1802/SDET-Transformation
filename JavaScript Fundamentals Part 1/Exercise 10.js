// Conditional ternary operator

const age = 25;
age >= 18 ? console.log('you can vote') : console.log('you cannot vote');

const age2 = age >= 18 ? 'you can vote' : 'you cannot vote';
console.log(age2);