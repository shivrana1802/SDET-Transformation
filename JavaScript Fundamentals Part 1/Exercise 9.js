// The Switch statement

const employeeName = 'Ayushi';

switch(employeeName){
    case 'Preethi' :
    console.log(`${employeeName} is a WBD employee`);
    break;
    case 'Ayushi' :
    console.log(`${employeeName} is a GL employee`);
    break;
    default: 
    console.log(`${employeeName} is an other company employee`);
}