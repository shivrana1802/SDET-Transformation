// Equality operator (===/== OR !==/!=)

// Problem - if name is exactly 'preethi' , print WBD employee ,

const employeeName = 'Preethi';

if (employeeName === 'Preethi'){
    console.log(`${employeeName} is a WBD employee`);
}
else{
    console.log(`${employeeName} is not a WBD employee`);
}